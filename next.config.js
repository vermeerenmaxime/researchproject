/** @type {import('next').NextConfig} */
module.exports = {
  reactStrictMode: true,
  swcMinify: true,
  experimental: {
    removeConsole: false,
    // reactRoot: true,
    // concurrentFeatures: true,
  },
};
